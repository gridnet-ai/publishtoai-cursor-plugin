# npm placeholders (historical)

These 0.0.1 stubs were used before `@publishtoai/cli@1.0.0` and `publishtoai@1.0.0` shipped.

**Do not publish from this folder.** Use the real packages:

- `npm publish -w @publishtoai/cli`
- `npm publish -w publishtoai`
